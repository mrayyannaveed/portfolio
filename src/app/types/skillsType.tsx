export interface Skill {
        head: string,
        desc: string,
        bgcol: string,
        atribute: React.ReactNode,
        ani: string
    }


export interface Project {
    image:string,
    head:string,
    desc:string,
    link:string
  }

export interface com {
    heading: string,
    desc: string,
    bgcol: string,
    atribute: React.ReactNode
}